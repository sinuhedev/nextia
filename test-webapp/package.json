{
  "name": "test-webapp",
  "description": "test-webapp",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite --mode dev",
    "clean": "rm -fr node_modules package-lock.json pnpm-lock.yaml .coverage out",
    "build": "vite build --mode",
    "preview": "vite preview",
    "format": "biome format",
    "lint": "biome lint",
    "check": "biome check --reporter=summary",
    "page": "npm create nextia page",
    "component": "npm create nextia component",
    "container": "npm create nextia container",
    "test": "vitest run",
    "test:name": "vitest run --testNamePattern",
    "test:silent": "vitest run --silent",
    "test:coverage": "vitest run --silent --coverage"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "@vitest/coverage-v8": "^4.0.18",
    "jsdom": "^29.0.1",
    "vite": "^8.0.1",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "nextia": "file:../",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
